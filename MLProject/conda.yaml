name: spotify_sentiment_env

channels:
  - defaults
  - conda-forge

dependencies:
  - python=3.10
  - pip
  - pip:
      - pandas
      - numpy
      - scikit-learn
      - imbalanced-learn
      - mlflow==2.19.0
      - joblib
      - sastrawi